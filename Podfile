# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Squirrel (iOS)' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Squirrel (iOS)

end

target 'Squirrel (macOS)' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Squirrel (macOS)

end

target 'Squirrel Extension (iOS)' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Squirrel Extension (iOS)
  pod "GCDWebServer", "~> 3.5"


end

target 'Squirrel Extension (macOS)' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Squirrel Extension (macOS)
  pod "GCDWebServer", "~> 3.5"


end
